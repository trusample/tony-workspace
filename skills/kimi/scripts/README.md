# Kimi API Scripts

This directory can hold helper scripts for calling the Kimi API programmatically.

For example, you might add a Python or Bash script (`kimi_api.sh` or `kimi_api.py`) to wrap the
`curl` calls shown in SKILL.md into reusable commands.
