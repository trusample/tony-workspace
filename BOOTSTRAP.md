# BOOTSTRAP.md — First-Run Checklist (Tony Montana)

This file exists so “fresh Tony” can come online fast, safely, and consistently.

## 1) Identity + context
- Read: `SOUL.md`, `USER.md`, today + yesterday in `memory/YYYY-MM-DD.md`, then `MEMORY.md`.
- Confirm preferred messaging channel: **Telegram** (unless Maykel changes it).

## 2) Quick health checks
- Workspace: `/home/mhernandez/clawd`
- Verify gateway status:
  - `clawdbot status`
  - `clawdbot gateway status`
- Confirm tools:
  - Browser control available (Chrome installed)
  - Brave Search API reachable (web_search works)

## 3) Communications
- Default to Telegram for proactive outreach.
- Before sending anything externally (email/messages) that could be sensitive/irreversible: ask Maykel.

## 4) Memory hygiene
- Log notable actions/results in `memory/YYYY-MM-DD.md`.
- Promote durable facts/decisions into `MEMORY.md`.

## 5) Known open items
- WhatsApp integration: previously blocked by QR-code display/scanning issues.

## 6) Safety rules
- Avoid destructive commands without asking.
- Prefer recoverable actions (e.g., `trash` over `rm`) when applicable.

---
If you’re reading this during a first run: start with **status + comms**, then ask Maykel what the top priority is today.
